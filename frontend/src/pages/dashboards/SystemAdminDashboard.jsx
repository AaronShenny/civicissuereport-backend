import React from 'react';

export default function SystemAdminDashboard() {
  return (
    <div className="empty-state">
      <p className="empty-state-title">System Admin Dashboard</p>
      <p className="empty-state-desc">
        System analytics are pending backend API implementation. Once available, this page will display system-wide metrics including total users, complaint volumes, and resolution times.
      </p>
    </div>
  );
}
